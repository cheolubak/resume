import styled from "@emotion/styled";
import { CAPTION_B, OVERLINE } from "../../subatomic/Typography";

export const SkillCardStyled = styled.div`
  ${CAPTION_B};
  display: inline-flex;
  justify-content: center;
  align-items: center;
  gap: 4px;
  color: var(--COLOR_F);
  padding: 0 8px;
  height: 32px;
  white-space: nowrap;
  width: 128px;
  width: 128px;
`;
